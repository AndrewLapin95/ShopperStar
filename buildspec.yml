version: 0.2


phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - sudo npm install --prefix server
      - sudo npm install --prefix server/client
  build:
    commands:
      - echo Build started on `date`
      - echo Compiling the Node.js code
      - npm run build
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - server/config/*
    - server/index.js
    - server/models/*
    - server/package.json
    - server/routes/*
    - server/services/*
    - server/client/build/*